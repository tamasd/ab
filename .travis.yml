language: go
go:
    - 1.5
addons:
    postgresql: "9.4"
services:
    - postgresql
before_script:
    - psql -c 'CREATE DATABASE abtest;' -U postgres
env:
    - PGCONNECTSTRING="dbname=abtest user=postgres sslmode=disable"
