language: go
go:
    - 1.7
addons:
    postgresql: "9.5"
services:
    - postgresql
before_script:
    - psql -c 'CREATE DATABASE abtest;' -U postgres
    - psql -c 'CREATE DATABASE abauthtest;' -U postgres
    - psql -c 'CREATE DATABASE absearchtest;' -U postgres
    - echo 'db = "dbname=abtest user=postgres sslmode=disable"' > test.toml
    - echo 'db = "dbname=abauthtest user=postgres sslmode=disable"' > services/auth/test.toml
    - echo 'db = "dbname=absearchtest user=postgres sslmode=disable"' > services/search/test.toml
